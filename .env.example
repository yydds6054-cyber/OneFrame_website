# Local development (SQLite). Used by prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# Production (PostgreSQL). Used by prisma/schema.production.prisma on Vercel.
# Set this in the Vercel dashboard, not here.
# DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
